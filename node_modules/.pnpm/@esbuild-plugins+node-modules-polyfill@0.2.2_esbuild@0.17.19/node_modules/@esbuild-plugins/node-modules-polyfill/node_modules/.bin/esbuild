#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/Users/ilasbikbulatov/Documents/Gemini/PyBot/fox-screener-main/node_modules/.pnpm/@esbuild-plugins+node-modules-polyfill@0.2.2_esbuild@0.17.19/node_modules/esbuild/bin/esbuild
