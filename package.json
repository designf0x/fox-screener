{
  "name": "fox-screener",
  "version": "1.0.0",
  "description": "Fox Market Screener Telegram Bot running on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:worker": "node tests/worker-runtime.cjs",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240502.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.55.0"
  }
}
